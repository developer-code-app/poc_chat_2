import 'package:flutter/material.dart';

class TopicsSubpage extends StatelessWidget {
  const TopicsSubpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}